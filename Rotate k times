#include<stdio.h>
#include <stdlib.h>

int main()
{
int n;
scanf("%d",&n);
int a[n],i,j,d;
long int k;
for(i=0;i<n;i++)
scanf("%d",&a[i]);
scanf("%ld",&k);
if(n<k)
d=n-(k%n);
else
d=n-k;
for(i=d;i<n;i++)
printf("%d ",a[i]);
for(i=0;i<d;i++)
printf("%d ",a[i]);

}
