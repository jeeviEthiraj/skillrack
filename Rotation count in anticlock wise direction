#include<stdio.h>
#include <stdlib.h>

int main()
{
int n;
scanf("%d",&n);
int a[n],i,min=0,res=0;
for(i=0;i<n;i++)
scanf("%d",&a[i]);
min=a[0];
for(i=0;i<n;i++)
{
    if(min>a[i])
    {
        min=a[i];
        res=i;
    }
}
printf("%d",res);

}
